[tool:pytest]
testpaths = tests
addopts = -ra --strict-markers -m "not integration"
markers =
    slow: test takes >2s
    integration: requires live network (VT_INTEGRATION=1)
    bash: shells out to bash scripts
